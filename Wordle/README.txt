This algorithm takes in a word with a list of 
0's and 1's that indicates what letters are wrong and correct. We 
are also given a list of strings to deduct the closest word that can 
match our given word. This algorithm primarily uses both counting 
and radix sort.